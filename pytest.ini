[pytest]
testpaths = cloud/tests local/tests
markers =
    integration: requires external PostgreSQL via docker compose (auto-skipped unless reachable)
filterwarnings =
    ignore::DeprecationWarning
